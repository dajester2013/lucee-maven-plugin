<component-set>
	<components>
		<!-- LAR -->
		<component>
			<role>org.apache.maven.lifecycle.mapping.LifecycleMapping</role>
			<role-hint>lar</role-hint>
			<implementation>
				org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
			</implementation>
			<configuration>
				<lifecycles>
					<lifecycle>
						<id>default</id>
						<phases>
							<process-resources>org.apache.maven.plugins:maven-resources-plugin:resources</process-resources>
							<compile>${project.groupId}:${project.artifactId}:${project.version}:lar-copy-sources</compile>
							<process-test-resources>org.apache.maven.plugins:maven-resources-plugin:testResources</process-test-resources>
							<!-- <test-compile>${project.groupId}:${project.artifactId}:${project.version}:testCompile</test-compile> -->
							<test>org.apache.maven.plugins:maven-surefire-plugin:test</test>
							<package>${project.groupId}:${project.artifactId}:${project.version}:lar</package>
							<install>org.apache.maven.plugins:maven-install-plugin:install</install>
							<deploy>org.apache.maven.plugins:maven-deploy-plugin:deploy</deploy>
						</phases>
					</lifecycle>
				</lifecycles>
			</configuration>
		</component>

		<component>
			<role>org.apache.maven.artifact.handler.ArtifactHandler</role>
			<role-hint>lar</role-hint>
			<implementation>
				org.apache.maven.artifact.handler.DefaultArtifactHandler
			</implementation>
			<configuration>
				<!--the extension used by Maven in the repository -->
				<extension>lar</extension>
				<!--the type used when specifying dependencies etc. -->
				<type>lar</type>
				<!--the packaging used when declaring an implementation of the packaging -->
				<packaging>lar</packaging>
			</configuration>
		</component>


		<!-- LEX -->
		<!-- <component>
			<role>org.apache.maven.lifecycle.mapping.LifecycleMapping</role>
			<role-hint>lex</role-hint>
			<implementation>
				org.apache.maven.lifecycle.mapping.DefaultLifecycleMapping
			</implementation>
			<configuration>
				<phases>
					use the basic jar lifecycle bindings, add additional executions 
						in here if you want anything extra to be run
					<process-resources>
						org.apache.maven.plugins:maven-resources-plugin:resources
					</process-resources>
					<package>
						org.apache.maven.plugins:maven-jar-plugin:jar
					</package>
					<install>
						org.apache.maven.plugins:maven-install-plugin:install
					</install>
					<deploy>
						org.apache.maven.plugins:maven-deploy-plugin:deploy
					</deploy>
				</phases>
			</configuration>
		</component>

		<component>
			<role>org.apache.maven.artifact.handler.ArtifactHandler</role>
			<role-hint>lex</role-hint>
			<implementation>
				org.apache.maven.artifact.handler.DefaultArtifactHandler
			</implementation>
			<configuration>
				the extension used by Maven in the repository
				<extension>lex</extension>
				the type used when specifying dependencies etc.
				<type>lex</type>
				the packaging used when declaring an implementation of the packaging
				<packaging>lex</packaging>
			</configuration>
		</component> -->
	</components>
</component-set>
